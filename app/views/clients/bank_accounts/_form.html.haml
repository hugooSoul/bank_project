= simple_form_for [client, bank_account] do |f|
    = f.input :account_number, input_html: { class: "form-control" }
    .form-actions
        = f.submit class: "btn btn-primary" do
            Save Bank Account
        = link_to client_path(client.id), class: "btn btn-primary" do
            Cancel